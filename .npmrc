auto-install-peers=false
strict-peer-dependencies=false
# apn@2.2.0 resolves its http2 dep from a GitHub tarball (node-apn/node-http2).
# pnpm 11+ blocks exotic subdeps by default; this restores the previous behaviour
# until apn is upgraded to a version that uses registry-only deps.
block-exotic-subdeps=false
